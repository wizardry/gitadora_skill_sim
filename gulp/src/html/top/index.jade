.contentWrap.indexPage
	.sectionBlock.loginFormBlock
		.loginBlock
			.loginBlockInner
				- if(isset($error)){
				p.errorMeesage
					- echo $error;
				- }
				- echo Form::open();
					dl.formFrame
						dt ユーザーIDもしくはメールアドレス
						dd
							- echo Form::input('id',Input::post('id'),array('required'=>'required','type'=>'text',));
					dl.formFrame
						dt パスワード
						dd
							- echo Form::input('password',Input::post('password'),array('required'=>'required','type'=>'password',));
					.buttonWrap
						label.submitBase.primary(for="loginSubmit") ログイン
							- echo Form::submit('submit_btn', 'ログイン',array('id'=>'loginSubmit'));
				- echo Form::close();
		.registBlock
			.registBlockInner
				p
					a(href="newregist/").linkButtonBase.newRegist 新規登録
	.sectionBlock.toSearchBlock
		p
			a(href="search/").linkButtonBase.toSearch 検索 

